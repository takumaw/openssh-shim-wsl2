# HISTORY

## 0.1.1

  * Embed Windows version resource metadata.
  * Harden release binaries with security exploit mitigations and static CRT.
  * Add a binary smoke test for local and CI validation.
  * Harden CI workflow permissions and enable Dependabot.

## 0.1.0

  * Initial release with `ssh.exe`, `scp.exe`, and `sftp.exe` shims for WSL2 OpenSSH.
