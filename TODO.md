# TODO

Future work:

* Add optional Windows ssh-agent bridging.
* Consider additional OpenSSH utilities such as `ssh-add.exe`, `ssh-agent.exe`, `ssh-keygen.exe`, and `ssh-keyscan.exe`.
* Consider rewriting paths inside `sftp -b` batch files.
* Improve `sftp.exe` parity if practical use cases require it.
* Consider an optional config file if command-line options and environment variables become insufficient.
* Consider additional path conversion backends if built-in conversion and `wslpath` are not enough.
